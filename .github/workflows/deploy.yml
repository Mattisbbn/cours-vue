name: CI/CD Pipeline

# Déclenchement de la pipeline lors d'un push sur la branche main
on:
  push:
    branches:
      - main

jobs:
  test:
    name: Build And Checks
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build Docker images
        run: docker compose -f docker-compose.yml build --pull

      - name: Backend sanity checks
        run: |
          docker compose run --rm back php -v
          docker compose run --rm back php bin/console doctrine:migrations:status --no-interaction

      - name: Frontend production build
        run: |
          cd front
          corepack enable
          pnpm install --frozen-lockfile
          pnpm run build

  deploy:
    name: Deploy to VPS
    needs: test
    runs-on: ubuntu-latest

    steps:
      - name: SSH deploy
        uses: appleboy/ssh-action@v1.2.5
        with:
          host: ${{ secrets.VPS_HOST || vars.VPS_HOST }}
          username: ${{ secrets.VPS_USER || vars.VPS_USER }}
          key: ${{ secrets.VPS_SSH }}
          port: ${{ secrets.VPS_PORT || vars.VPS_PORT || '22' }}
          script: |
            set -e
            cd ${{ secrets.VPS_APP_PATH || vars.VPS_APP_PATH }}

            if docker compose version >/dev/null 2>&1; then
              COMPOSE="docker compose"
            elif command -v docker-compose >/dev/null 2>&1; then
              COMPOSE="docker-compose"
            else
              echo "No docker compose command found on VPS"
              exit 1
            fi

            # Sync code
            git fetch --all
            git checkout main
            git pull --ff-only origin main

            # Rebuild and restart
            $COMPOSE build --pull back front
            $COMPOSE up -d --force-recreate back front

            # Run migrations if needed
            $COMPOSE exec -T back php bin/console doctrine:migrations:migrate --no-interaction --allow-no-migration

            # Cleanup old dangling images
            docker image prune -f
